import styled from 'styled-components';

export const ExperienceStyle = styled.div`
  padding: 20px;
  position: relative;
  z-index: 10;

  .card {
    max-width: 750px;
    margin: 0 auto;
    width: 100%;
    padding: 15px;
  }
`;
